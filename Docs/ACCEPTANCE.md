# Consolepilot 验收记录

更新日期：2026-08-29

状态：`[ ]` 未验收，`[x]` 已通过，`[!]` 失败待修，`[-]` 尚未实现。

## A. 当前 Mock 核心链路

- [x] A1 App 正常启动并显示主窗口。
- [x] A2 普通消息由本地 Mock 分块流式返回，不访问真实 API。
- [x] A3 `/code` 的代码块、CJK、English、emoji 无乱码。
- [x] A4 `/long` 在用户位于底部时持续跟随最新内容。
- [x] A5 `/long` 中主动上翻后不强制抢回底部。
- [x] A6 Control+C 中断后已收内容保留，输入恢复。
- [x] A7 同一会话连续三轮消息顺序正确。
- [x] A8 新建第二会话并切换，内容互不串线。
- [x] A9 Command+Q 重启后历史完整恢复。
- [x] A10 新建按钮可见、标题左对齐、滚动条无白色轨道。
- [x] A11 Sessions 侧栏可隐藏、可调宽度，窄屏自动收起。
- [x] A12 Command+Q 正常退出；Command+W 隐藏后可重开。
- [x] A13 单条会话删除：确认弹窗出现，确认后会话及消息消失并自动切换。
- [x] A14 清空全部会话：确认弹窗出现，确认后列表和对话区清空，可重新发起会话。
- [x] A15 聊天布局：用户消息右对齐、助手消息左对齐，多轮排布稳定。
- [x] A16 Markdown：标题、列表、行内代码和围栏代码块正确渲染，流式结束后样式完整。
- [x] A17 性能：展开侧栏、切换含长消息的会话、发送 `/long` 均无明显卡顿。
- [x] A18 分页：长会话初始显示最近记录；滚到顶部可继续加载更早记录且阅读位置不跳。

## 最近回归修复（待复测）

- [x] A19 中断流切换恢复：中断后的已接收内容已写入数据库；早期 token 在取消竞态下也可恢复，且切换不重复渲染。
- [x] A20 重启恢复：退出前保存活动流检查点并使用稳定会话排序；未完成回复和列表顺序已人工复测通过。
- [x] A21 Mock 回复不重复回显用户输入；助手回复内容保持独立。
- [x] A22 聊天气泡快捷复制：每个气泡右下角复制按钮可将原始 Markdown 正文写入剪贴板。
- [x] A23 复制按钮交互：悬停显示手型光标，按钮叠加在气泡内不额外占用一行。
- [x] A24 复制按钮避让：助手/用户消息最后一行内容不与右下角复制按钮重叠。
- [x] A25 复制按钮悬停：鼠标未点击时悬停即显示手型光标，且长行文字按按钮避让宽度换行。
- [x] A26 `consolepilot run ask <input>`：App 当前会话实时显示助手 Mock 回复，不仅保存用户消息。
- [x] A27 App 菜单“设置…”：可读取、编辑并保存 `~/.config/consolepilot/config.toml`，非法 TOML 不替换当前有效配置。
- [x] A28 App 菜单“打开辅助功能设置”：菜单入口可打开系统设置，侧栏不再显示权限按钮。
- [x] A29 Keychain：首次允许后重启 App、CLI 与替换 Release 构建授权行为已完成实机复测；ad-hoc 重签名提示属于 macOS 签名限制。
- [ ] A30 设置编辑器：去除左侧说明栏，配置内容内含中文注释和 Profile/Action/Tail 全字段参考，待人工复测。
- [ ] A31 设置编辑器：鼠标任意位置可编辑，长内容有滚动条，支持标准选择、复制粘贴和撤销，待人工复测。
- [ ] A32 设置保存：保存前校验 TOML 和配置规则；保存、放弃、重新读取按钮状态清晰，关闭未保存内容会确认，待人工复测。
- [ ] A33 设置操作栏：保存更改、放弃更改、重新读取三个按钮保持相同固定宽度，保存前后不发生尺寸跳变，待人工复测。
- [ ] A35 用量统计菜单：App 菜单可打开今日、近 7 天和全部用量窗口，仅显示请求数、输入/输出 Token，不统计金额，待人工复测。
- [ ] A36 用量统计可读性：窗口使用深色背景和高对比度文字，标题、统计行和刷新按钮均清晰可见，待人工复测。
- [ ] A37 核心 Action 主通道：其它 App 选中文字后直接触发配置快捷键（无需预先手动 `⌘C`），App 向触发瞬间的原前台进程定向静默模拟复制、按预设模板创建新会话并在副屏流式展示回复，不抢占主屏前台，待人工复测。
- [ ] A38 Action 捕获降级与安全输入：AX/模拟复制按配置顺序工作，模拟复制必须确认剪贴板发生变化；选区 Action 不使用历史剪贴板内容，剪贴板恢复；无选区、排除应用和安全输入均给出明确提示且不发起请求，待人工复测。
- [ ] A39 全局快捷键热重载：Action 快捷键可注册；配置保存后旧组合失效、新组合立即生效，空快捷键不注册，待人工复测。
- [ ] A40 菜单栏与快速提问：菜单栏图标可显示/隐藏主窗口；快速提问面板可提交问题并在当前会话流式显示回复，待人工复测。
- [ ] A41 Profile 切换：应用菜单可列出配置中的 Profile，切换后新对话使用所选 Profile；远程 Profile 仅从 Keychain/环境变量读取密钥，待人工复测。
- [ ] A42 输入历史：Option+↑/↓ 可浏览最近输入，重启后最近 100 条仍可用，待人工复测。
- [ ] A43 ChatAnywhere 真实 SSE：长回复按连续 SSE delta 实时增长，不出现首段后长时间空白或仅完成时一次性渲染；中断/切换/重启保留已接收前缀，待最新构建复测。
- [ ] A44 Loading 与完成指标：流开始显示连接状态；首个真实 delta 后清除占位；结束后显示字符数、首字延迟、总耗时和结束原因，待最新构建复测。
- [x] A45 完成状态：Mock/真实流结束后，状态栏按会话持续显示最终字符数、首字延迟、总耗时和 `finish_reason`；切换会话再回来仍可见，直到该会话开始下一轮生成，不依赖短暂计时。
- [x] A34 设置保存崩溃：确认 TOMLDecoder 0.3 的整数解码与编辑器字符串桥接缺陷；现已在解析入口规范化 UTF-8、整数先解码 Int，并完成实机合法/非法保存复测，App 不再崩溃。

## B. 自动化与产物证据

- [x] B1 `swift test`：73 项通过，0 失败（2026-08-29）。
- [x] B2 `./Scripts/build-local.sh` 生成 `dist/Consolepilot.app` 与 CLI。
- [x] B3 `codesign --verify --deep --strict` 通过。
- [x] B4 最终 Release 窗口截图确认历史长文本实际可见（2026-08-28）。
- [-] B5 D1–D8 工具门禁全绿。
- [-] B6 R1–R24 人工审查签字。
- [-] B7 T1–T5 模拟/性能/故障/生命周期测试完成。

## C. 最终实机 P1–P10

- [-] P1 安装与首次启动（1–8）。
- [-] P2 配置系统（9–20）。
- [-] P3 十五 App 文本捕获矩阵（21–37）。
- [-] P4 快捷键与窗口（38–56）。
- [-] P5 流式渲染完整验收（57–68）；当前只执行 A 组 Mock 子集。
- [-] P6 对话与输入（69–76）；当前只执行 A 组 Mock 子集。
- [-] P7 数据与用量（77–82）；当前只完成基础持久化验证。
- [x] P8 LocalServer 与 CLI（83–88）：HTTP 鉴权、`ask/run/tail`、轮转与 App 会话切换已完成验收。
- [ ] P8.1 不存在日志文件：`consolepilot tail <missing>` 返回明确错误（HTTP 422），不会创建空会话，待人工复测。
- [-] P9 稳定性与权限持久性（89–95）。
- [-] P10 交付复核（96–100）。

完整编号与通过标准以 `Consolepilot_plan_v4.0.md` 第 10 部分为准。
